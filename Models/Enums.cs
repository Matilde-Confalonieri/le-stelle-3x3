namespace ThreeByThreeManager.Models;

public enum MatchType { Group, Quarter, Semi, Final }
public enum MatchStatus { Scheduled, Played }
public enum SchemeCategory { Offense, Defense, Inbound, Special }
